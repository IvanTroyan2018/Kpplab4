extends layout

block main
    if username
        h1 Привіт,  #{username}!!
        a(href='/suggestions') Переглянути  пропозиції
    else
        h1 Вхід:

        form(method = 'post')
            input(type='text' name='username')
            button(type='submit') Увійти