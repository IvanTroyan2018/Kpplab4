doctype html

html(lang='ru')
    head
        meta(charset='utf-8')
        title Список пропозицій
        link(rel='stylesheet' href='/style.css')

        body
            main.page
                if message
                    p.flash_message= message
                block main